import { createSlice } from "@reduxjs/toolkit";

const products = createSlice({
    name : "products",
    initialState : {
        productList : []
    },
    reducers : {
        
    }
})

export default products